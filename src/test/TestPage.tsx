import { CardDealUnloaded } from '@/components/common/Card';

const TestPage = () => {
    return (
        <div style={{ padding: '1rem', display: 'grid', gap: '2rem', maxWidth: '500px' }}>
            <CardDealUnloaded />
        </div>
    );
};

export default TestPage;
